name: qr_code_usage
description: WiserSenseQR Scan
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  qr_flutter: ^4.0.0
  flutter_barcode_scanner: ^2.0.0
  url_launcher: ^6.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/flutter_logo.png
